# Monitor Runner

Go-based monitor runner that fetches, tests, and saves monitor checks using bun for PostgreSQL queries.

## Usage

```bash
# Build the runner
npm run runner:build

# Run the runner
npm run runner

# Or manually
cd runners
go build -o runners
./runners
```
